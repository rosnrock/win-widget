<UserControl x:Class="WinWidget.Views.CalendarWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="MonthLabel" Margin="2,0,0,12" Style="{StaticResource WidgetEyebrow}" />
        <UniformGrid x:Name="WeekdayGrid" Grid.Row="1" Columns="7" Margin="0,0,0,5" />
        <UniformGrid x:Name="DaysGrid" Grid.Row="2" Columns="7" Rows="6" />
    </Grid>
</UserControl>
