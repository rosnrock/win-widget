<UserControl x:Class="WinWidget.Views.CalendarWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <StackPanel Width="224">
        <TextBlock x:Name="MonthLabel" Margin="2,0,0,10"
                   Style="{StaticResource WidgetLabel}" FontSize="15" />
        <UniformGrid x:Name="WeekdayGrid" Columns="7" Margin="0,0,0,5" />
        <UniformGrid x:Name="DaysGrid" Columns="7" Rows="6" />
    </StackPanel>
</UserControl>
