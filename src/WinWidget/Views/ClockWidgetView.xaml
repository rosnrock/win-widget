<UserControl x:Class="WinWidget.Views.ClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <Grid Margin="3,0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="DateLabel" HorizontalAlignment="Center" TextAlignment="Center"
                       Style="{StaticResource WidgetEyebrow}" />
            <TextBlock x:Name="TimeLabel" Margin="0,-10,0,-7"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       FontFamily="{StaticResource WidgetFont}"
                       FontSize="108" FontWeight="ExtraBold"
                       TextOptions.TextFormattingMode="Display" />
            <TextBlock x:Name="DayHintLabel" HorizontalAlignment="Center" TextAlignment="Center"
                       Style="{StaticResource WidgetLabel}" FontSize="14" Opacity="0.58" />
        </StackPanel>
    </Grid>
</UserControl>
