<UserControl x:Class="WinWidget.Views.ClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="DateLabel" Style="{StaticResource WidgetLabel}"
                   HorizontalAlignment="Center" FontSize="27" />
        <TextBlock x:Name="TimeLabel" Margin="0,-10,0,0"
                   HorizontalAlignment="Center"
                   FontFamily="{StaticResource WidgetFont}"
                   FontSize="124" FontWeight="Bold"
                   TextOptions.TextFormattingMode="Display" />
    </StackPanel>
</UserControl>
