<UserControl x:Class="WinWidget.Views.ImageWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid ClipToBounds="True">
        <Image x:Name="Photo" Stretch="UniformToFill" />
        <TextBlock x:Name="StatusText" Text="Choose an image in WinWidget settings"
                   Margin="24" TextAlignment="Center" TextWrapping="Wrap"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontWeight="SemiBold" Opacity="0.78" />
    </Grid>
</UserControl>
