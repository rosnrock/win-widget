<UserControl x:Class="WinWidget.Views.WeatherWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <Grid Margin="3,0" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="LocationLabel"
                   Style="{StaticResource WidgetEyebrow}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextTrimming="CharacterEllipsis" />
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="WeatherIcon"
                       Margin="0,0,14,0"
                       VerticalAlignment="Center"
                       FontFamily="Segoe UI Emoji"
                       FontSize="42" />
            <TextBlock x:Name="TemperatureLabel"
                       Grid.Column="1"
                       VerticalAlignment="Center"
                       FontFamily="{StaticResource WidgetFont}"
                       FontSize="76"
                       FontWeight="SemiBold"
                       TextOptions.TextFormattingMode="Display" />
        </Grid>
        <StackPanel Grid.Row="2" HorizontalAlignment="Center">
            <TextBlock x:Name="ConditionLabel"
                       MaxWidth="390"
                       Style="{StaticResource WidgetLabel}"
                       FontSize="15"
                       TextAlignment="Center"
                       TextTrimming="CharacterEllipsis" />
            <TextBlock x:Name="RangeLabel"
                       Margin="0,2,0,0"
                       Style="{StaticResource WidgetLabel}"
                       FontSize="14"
                       Opacity="0.78"
                       TextAlignment="Center" />
            <TextBlock x:Name="StatusLabel"
                       MaxWidth="390"
                       Margin="0,2,0,0"
                       FontFamily="{StaticResource WidgetTextFont}"
                       FontSize="11"
                       Opacity="0.58"
                       TextAlignment="Center"
                       TextTrimming="CharacterEllipsis" />
        </StackPanel>
    </Grid>
</UserControl>
